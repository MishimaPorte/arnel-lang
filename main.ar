export main = (): int -> {
    print("kek: %zu\n", is_odd(1));
    return 0;
};

export is_odd = (digit: int): int -> {
    return if ((digit / 2) * 2 != digit) {
        1;
    } else {
        0;
    };
};
